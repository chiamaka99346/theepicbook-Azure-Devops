# IMPORTANT: NEXT_PUBLIC_ variables are exposed to the browser.
# Never put secrets here. Only use public-facing URLs.

NEXT_PUBLIC_API_URL=http://{{ frontend_public_ip }}/api

# Add any other frontend env vars your EpicBook app needs below
